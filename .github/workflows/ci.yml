name: Bundled extensions on PostgreSQL 18 and 19
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  fdw:
    uses: ./.github/workflows/fdw.yml
  functional:
    uses: ./.github/workflows/functional.yml
