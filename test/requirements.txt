# The supported test environment is the repo `nix-shell`, which provides the
# current nixpkgs versions of the Python test dependencies.
#
# If you need a non-Nix environment, install the top-level test tools and let
# your resolver select compatible versions.
pytest
testgres
